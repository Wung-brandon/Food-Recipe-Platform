from django.urls import path
from .views import *

urlpatterns = [
    # path("recipe/", RecipeList.as_view(), name="recipe-list"),
    # path("recipe/<int:pk>/", RecipeDetail.as_view(), name="recipe-detail"),
    # path("recipe/<int:pk>/like/", RecipeLike.as_view(), name="recipe-like"),
    # path("recipe/<int:pk>/unlike/", RecipeUnlike.as_view(), name="recipe-unlike"),
    # path("recipe/<int:pk>/comment/", RecipeComment.as_view(), name="recipe-comment"),
]
